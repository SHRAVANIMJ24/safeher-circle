package com.safeher.backend.entity;

public enum OrgType {
    /** A phone line, usually government-run and national. */
    HELPLINE,
    /** A non-governmental organisation offering direct support. */
    NGO,
    /** Somewhere to stay. */
    SHELTER,
    /** Free or subsidised legal advice. */
    LEGAL_AID,
    /** A police unit, typically a women's cell. */
    POLICE
}
